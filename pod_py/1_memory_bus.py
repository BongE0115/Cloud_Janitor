import time
import sys

def draw_truck(box_count):
    # 박스 개수에 따라 트럭 짐칸을 채움
    boxes = "[#]" * (box_count % 11)
    empty = "   " * (10 - (box_count % 11))
    
    print("\n--- [정상] 메모리 물류 트럭 가동 중 ---")
    print(f"   ______________________      ")
    print(f"  |{boxes}{empty}|__    ")
    print(f"  |______________________| |_  ")
    print(f"    (O)              (O)    O= ")
    print(f" [ 메모리 적재량: {box_count} Blocks ]")

print("메모리 창고 가동 시작...")
storage = []
count = 0

while True:
    # 1. 메모리 점유 (약 15MB씩 리스트에 추가)
    # 리소스 제한을 고려해 60개까지만 쌓고 유지합니다.
    if len(storage) < 60:
        storage.append("📦" * (5 * 10**6)) 
        count += 1
    
    # 2. 시각화 출력
    # 로그가 너무 길어지지 않게 구분선을 넣습니다.
    print("-" * 35)
    draw_truck(count)
    print("상태: 데이터 보관 및 유지 중 (Active)")
    
    # 3. 유지 및 대기
    # 메모리는 한 번 할당하면 유지되므로 3초마다 체크합니다.
    time.sleep(3)